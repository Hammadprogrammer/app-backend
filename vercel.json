{
  "version": 2,
  "installCommand": "cd backend && npm install",
  "buildCommand": "cd backend && npm run prisma:generate && npm run build",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
